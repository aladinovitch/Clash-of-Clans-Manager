<UserControl x:Class="ClashOfClansManager.Views.ViewCurrentWar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClashOfClansManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <FlowDocumentScrollViewer Margin="5" BorderBrush="LightBlue" BorderThickness="3">
            <FlowDocument IsOptimalParagraphEnabled="True" IsHyphenationEnabled="True" TextAlignment="Left">
                <!--<Paragraph TextAlignment="Center">
                    <InlineUIContainer>
                        <StackPanel>
                            <Image Source="{Binding ViewModel.Clan.BadgeUrls.Medium}" Width="100"/>
                            --><!--<TextBlock Text="{Binding ViewModel.Clan.Name, StringFormat=Name : \{0\}}"/>--><!--
                            <TextBlock Text="{Binding ViewModel.Clan.Name}"/>
                            <TextBlock Text="{Binding ViewModel.Clan.Type}"/>
                            <TextBlock Text="{Binding ViewModel.Clan.Tag}"/>
                            <TextBlock Text="{Binding ViewModel.Clan.Description}"/>
                        </StackPanel>
                    </InlineUIContainer>
                </Paragraph>
                <Paragraph>
                    <Grid>
                        <ListView ItemsSource="{Binding ViewModel.Clan.MemberList}">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Icon">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <Image Source="{Binding League.IconUrls.Small}" Width="50"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                                    <GridViewColumn Header="Donate" DisplayMemberBinding="{Binding Donations}"/>
                                    <GridViewColumn Header="Req" DisplayMemberBinding="{Binding DonationsRecieved}"/>
                                    <GridViewColumn Header="Role" DisplayMemberBinding="{Binding Role}"/>
                                    <GridViewColumn Header="League" DisplayMemberBinding="{Binding League.Name}"/>
                                    <GridViewColumn Header="Trophy" DisplayMemberBinding="{Binding Trophies}"/>
                                    <GridViewColumn Header="VTrophy" DisplayMemberBinding="{Binding VersusTrophies}"/>
                                    <GridViewColumn Header="XP" DisplayMemberBinding="{Binding ExpLevel}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>
                </Paragraph>-->
                <Paragraph>
                    <InlineUIContainer>
                        <TextBlock Text="{Binding ViewModel.CurrentWar}" TextWrapping="WrapWithOverflow"/>
                    </InlineUIContainer>
                </Paragraph>
            </FlowDocument>
        </FlowDocumentScrollViewer>
    </Grid>
</UserControl>
